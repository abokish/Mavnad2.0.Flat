#pragma once

// System mode enumeration
enum class SystemMode {
  Stop,
  Cool,
  Heat,
  Regenerate,
  Manual,
  Experiment
};

// Air valve mode enumeration
enum class AirValveMode {
  Close,
  Open
};

// Watering mode enumeration
enum class WateringMode {
  Off,
  On
};
